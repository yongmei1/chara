<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.authapptutorial">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Authapptutorial">
        <activity android:name=".main_navigation.MainChatbot" />
        <activity android:name=".main_navigation.MainChatbot2" />
        <activity android:name=".main_navigation.Library" />
        <activity android:name=".main_navigation.List" />
        <activity android:name=".EditAccount" />
        <activity android:name=".Login" />
        <activity android:name=".Register"/>
        <activity android:name=".list.AddTask"/>
        <activity android:name=".list.ListTasks"/>
        <activity android:name=".list.EditTask"/>
        <activity android:name=".Chara"/>
        <activity android:name=".list.ViewTaskDetails"/>
        <activity android:name=".calendar.Calendar">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main_navigation.Account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>